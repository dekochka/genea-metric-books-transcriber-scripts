---
phase: 01-cross-platform-timeout-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - transcribe.py
autonomous: true
requirements:
  - SIGNAL-01
  - SIGNAL-02
  - SIGNAL-03
  - SIGNAL-04
  - QUALITY-04

must_haves:
  truths:
    - "transcribe.py imports successfully on Windows without AttributeError from signal.SIGALRM"
    - "TimeoutContext raises TimeoutError when operation exceeds timeout_seconds"
    - "TimeoutContext cancels timer when operation completes successfully"
    - "Timer resources are cleaned up in both success and failure paths"
  artifacts:
    - path: "transcribe.py"
      provides: "TimeoutContext class for cross-platform timeout detection"
      contains: "class TimeoutContext"
      min_lines: 30
    - path: "transcribe.py"
      provides: "Updated transcribe_image() using threading.Timer instead of signal.SIGALRM"
      contains: "with TimeoutContext"
      pattern: "threading\\.Timer"
  key_links:
    - from: "transcribe.py:transcribe_image()"
      to: "TimeoutContext.__enter__"
      via: "context manager entry"
      pattern: "with TimeoutContext\\(timeout_seconds"
    - from: "TimeoutContext._on_timeout()"
      to: "transcribe_image() exception handler"
      via: "TimeoutError raised by __exit__"
      pattern: "except.*TimeoutError"
---

<objective>
Replace Unix-only signal.SIGALRM with cross-platform threading.Timer for API call timeouts.

Purpose: Enable Windows users to run the transcription tool without AttributeError crashes from signal.SIGALRM. This is the critical blocker affecting all Windows users.

Output: Updated transcribe.py with TimeoutContext class and refactored transcribe_image() function using cross-platform timeout mechanism, preserving exponential backoff behavior (60s → 120s → 300s).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cross-platform-timeout-fix/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@transcribe.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement TimeoutContext class</name>
  <files>transcribe.py</files>
  <action>
Create TimeoutContext class as a context manager for cross-platform timeout detection. Insert this class near the top of transcribe.py after imports, before existing function definitions (around line 400).

Implementation requirements:
1. __init__(self, timeout_seconds, operation_name): Store timeout duration and operation name for error messages
2. __enter__(self): Start timer as daemon thread, record start_time, return self
3. __exit__(self, exc_type, exc_val, exc_tb): Cancel timer, check timed_out flag, raise TimeoutError if timeout occurred with elapsed time in message
4. _on_timeout(self): Callback that sets timed_out flag when timer fires
5. Set timer.daemon = True to prevent blocking program exit

Follow research pattern from 01-RESEARCH.md lines 367-399 (TimeoutContext implementation).

DO NOT modify transcribe_image() function in this task - that happens in Task 2.

Add docstring following Google style (see CONVENTIONS.md):
"""Context manager for cross-platform operation timeout using threading.Timer.

Replaces Unix-only signal.SIGALRM with cross-platform threading approach.
Timer runs in daemon thread and sets timeout flag if operation exceeds duration.
Automatic cleanup via __exit__ ensures timer.cancel() always called.

Args:
    timeout_seconds: Maximum duration in seconds before timeout
    operation_name: Descriptive name for timeout error messages

Raises:
    TimeoutError: If operation exceeds timeout_seconds

Example:
    try:
        with TimeoutContext(60, "API call"):
            response = api_client.call()
    except TimeoutError:
        # Handle timeout
"""
  </action>
  <verify>
Read transcribe.py and confirm:
1. TimeoutContext class exists with all required methods (__init__, __enter__, __exit__, _on_timeout)
2. Class is located before transcribe_image() function
3. Docstring explains purpose and cross-platform rationale
4. No syntax errors: python3 -m py_compile transcribe.py
  </verify>
  <done>
TimeoutContext class exists in transcribe.py with context manager protocol implemented, timer set as daemon thread, automatic cleanup in __exit__, and comprehensive docstring documenting cross-platform purpose.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace signal-based timeout with TimeoutContext</name>
  <files>transcribe.py</files>
  <action>
Refactor transcribe_image() function (lines 2737-2966) to use TimeoutContext instead of signal.SIGALRM.

Changes required:
1. Remove signal import at top of function (line 2738: "import signal")
2. Remove timeout_handler function definition (lines 2809-2813)
3. Replace signal.signal() and signal.alarm() calls with TimeoutContext usage:

BEFORE (lines 2812-2828):
```python
signal.signal(signal.SIGALRM, timeout_handler)
signal.alarm(timeout_seconds)
try:
    response = genai_client.models.generate_content(...)
    signal.alarm(0)  # Cancel
except ...:
    signal.alarm(0)  # Cleanup
```

AFTER:
```python
try:
    with TimeoutContext(timeout_seconds, f"Vertex AI call for {file_name}") as timeout_ctx:
        response = genai_client.models.generate_content(...)
except TimeoutError as e:
    # Existing retry logic continues
```

4. Keep all existing retry logic, exponential backoff (timeout_seconds_list = [60, 120, 300]), and exception handling unchanged
5. Keep logging statements unchanged
6. Verify no other signal.alarm() calls exist in transcribe_image()

DO NOT modify function signature, return values, or business logic - only replace timeout mechanism.

Add inline comment above TimeoutContext usage:
"# Cross-platform timeout using threading.Timer (replaces Unix-only signal.SIGALRM)"

Per QUALITY-04: Document threading.Timer timeout mechanism in code comments.
  </action>
  <verify>
1. Read transcribe_image() function and confirm signal imports removed
2. Verify TimeoutContext used in retry loop with correct timeout_seconds
3. Check exponential backoff preserved: timeout_seconds_list = [60, 120, 300]
4. Grep for signal usage: grep -n "signal\\." transcribe.py | grep -v "resume-signal" (should show no signal.SIGALRM or signal.alarm)
5. Syntax check: python3 -m py_compile transcribe.py
  </verify>
  <done>
transcribe_image() function uses TimeoutContext for timeout detection, signal.SIGALRM completely removed, exponential backoff behavior preserved (60s, 120s, 300s), retry logic unchanged, code compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. Import test: python3 -c "from transcribe import TimeoutContext; print('Import successful')"
2. Signal removal check: grep -E "(signal\\.SIGALRM|signal\\.alarm)" transcribe.py (should return nothing)
3. Threading usage check: grep "threading\\.Timer" transcribe.py (should find TimeoutContext implementation)
4. Syntax validation: python3 -m py_compile transcribe.py
5. Function signature unchanged: grep -A 2 "def transcribe_image" transcribe.py (verify parameters unchanged)
</verification>

<success_criteria>
1. transcribe.py imports successfully without referencing signal.SIGALRM
2. TimeoutContext class implements full context manager protocol (__enter__, __exit__)
3. Timer is set as daemon thread (daemon=True)
4. transcribe_image() uses TimeoutContext in retry loop with exponential backoff timeouts
5. All signal module references removed from timeout logic
6. Code compiles without syntax errors
7. Existing retry behavior preserved (3 attempts, 60s/120s/300s timeouts)
8. QUALITY-04 satisfied: threading.Timer mechanism documented in inline comments
</success_criteria>

<output>
After completion, create `.planning/phases/01-cross-platform-timeout-fix/01-01-SUMMARY.md`
</output>
