---
phase: 01-cross-platform-timeout-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - requirements.txt
  - tests/unit/test_timeout_mechanism.py
  - tests/integration/test_timeout_integration.py
autonomous: true
requirements:
  - SIGNAL-05
  - SIGNAL-06
  - SIGNAL-07
  - SIGNAL-08

must_haves:
  truths:
    - "Unit tests verify TimeoutContext raises TimeoutError after timeout_seconds"
    - "Unit tests verify timer cancels on successful completion"
    - "Integration tests verify retry logic with timeout on first attempt"
    - "Integration tests verify behavior when all retries timeout"
    - "Tests run fast using time mocking (no real delays)"
  artifacts:
    - path: "requirements.txt"
      provides: "Test dependencies for timeout testing"
      contains: "pytest-mock"
      min_lines: 1
    - path: "tests/unit/test_timeout_mechanism.py"
      provides: "Unit tests for TimeoutContext class"
      exports: ["test_timeout_fires_after_duration", "test_timeout_canceled_on_success", "test_timer_cancel_called_on_exit", "test_timeout_preserves_exponential_backoff"]
      min_lines: 80
    - path: "tests/integration/test_timeout_integration.py"
      provides: "Integration tests for retry logic with timeouts"
      exports: ["test_retry_logic_with_timeout_on_first_attempt", "test_all_retries_exhausted_with_timeout"]
      min_lines: 60
  key_links:
    - from: "tests/unit/test_timeout_mechanism.py"
      to: "transcribe.TimeoutContext"
      via: "direct import and instantiation"
      pattern: "from transcribe import TimeoutContext"
    - from: "tests/integration/test_timeout_integration.py"
      to: "transcribe.transcribe_image"
      via: "mocked API client with timeout simulation"
      pattern: "@patch\\('transcribe\\.transcribe_image'\\)"
---

<objective>
Create comprehensive unit and integration tests for the cross-platform timeout mechanism.

Purpose: Verify TimeoutContext works correctly (raises timeout, cancels on success, cleans up resources) and integrates properly with retry logic (preserves backoff, handles retry exhaustion). Tests must be fast (use time mocking) and deterministic.

Output: Test files with pytest markers for categorization, test dependencies added to requirements.txt, all tests passing locally.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cross-platform-timeout-fix/01-RESEARCH.md
@.planning/phases/01-cross-platform-timeout-fix/01-01-SUMMARY.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@transcribe.py
@tests/conftest.py
@tests/unit/test_ai_clients.py
@.pytest.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test dependencies to requirements.txt</name>
  <files>requirements.txt</files>
  <action>
Add testing dependencies required for timeout mechanism tests to requirements.txt.

Append these lines (maintain alphabetical order within testing section if one exists):
```
pytest-mock>=3.14.0
pytest-timeout>=2.3.0
freezegun>=1.5.0
```

These dependencies provide:
- pytest-mock: Simplifies mock creation and patching
- pytest-timeout: Protects test suite from hanging tests
- freezegun: Fast, deterministic time mocking for timeout tests

From 01-RESEARCH.md lines 46-53, these are standard testing libraries for timeout mechanism validation.

DO NOT modify existing dependencies or remove any lines - only add new test dependencies.

If requirements.txt has comments or sections, add to the testing/dev dependencies section. If no sections exist, add at the end.
  </action>
  <verify>
1. Read requirements.txt and confirm three new lines added
2. Check syntax: pip install --dry-run -r requirements.txt (or python3 -m pip check after install)
3. Verify no existing dependencies removed or modified
  </verify>
  <done>
requirements.txt contains pytest-mock>=3.14.0, pytest-timeout>=2.3.0, freezegun>=1.5.0 entries without modifying existing dependencies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for TimeoutContext</name>
  <files>tests/unit/test_timeout_mechanism.py</files>
  <action>
Create tests/unit/test_timeout_mechanism.py with unit tests verifying TimeoutContext behavior.

Structure (following TESTING.md patterns):
1. Import section: pytest, time, unittest.mock (Mock, patch), freezegun, transcribe.TimeoutContext
2. Test class: class TestTimeoutContext with pytest.mark.unit decorator
3. Four test methods (requirements SIGNAL-05, SIGNAL-06):

test_timeout_fires_after_duration():
- Use real time.sleep(2.0) to simulate slow operation
- Wrap in TimeoutContext(1, "test operation")
- Assert pytest.raises(TimeoutError, match="timed out after 1s")
- Verifies SIGNAL-05 (timeout fires correctly after specified duration)

test_timeout_canceled_on_success():
- Simulate fast operation with time.sleep(0.1)
- Wrap in TimeoutContext(2, "test operation")
- Should NOT raise TimeoutError
- Assert operation completes successfully
- Verifies SIGNAL-06 (timer cancels on successful API response)

test_timer_cancel_called_on_exit():
- Use @patch('threading.Timer') to mock Timer class
- Create mock timer instance
- Enter and exit TimeoutContext immediately
- Assert mock_timer.cancel.assert_called_once()
- Verifies SIGNAL-04 (cleanup on success path)

test_timeout_preserves_exponential_backoff():
- Verify timeout values [60, 120, 300] match exponential backoff
- Create TimeoutContext for each timeout value
- Assert ctx.timeout_seconds matches expected value
- Verifies SIGNAL-02 (exponential backoff preserved)

Follow patterns from tests/unit/test_ai_clients.py for structure and mocking style.

Use descriptive docstrings (Google style) for each test explaining what's verified and why.

Add @pytest.mark.unit decorator to all tests per TESTING.md conventions.
  </action>
  <verify>
1. Read tests/unit/test_timeout_mechanism.py and confirm 4 test methods exist
2. Run unit tests only: pytest tests/unit/test_timeout_mechanism.py -v
3. Verify all tests pass
4. Check test markers: pytest tests/unit/test_timeout_mechanism.py -v -m unit (should run all 4 tests)
  </verify>
  <done>
tests/unit/test_timeout_mechanism.py exists with 4 passing unit tests verifying timeout firing, timer cancellation, cleanup, and exponential backoff preservation. All tests marked with @pytest.mark.unit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create integration tests for retry logic with timeouts</name>
  <files>tests/integration/test_timeout_integration.py</files>
  <action>
Create tests/integration/test_timeout_integration.py with integration tests verifying retry logic interacts correctly with timeout mechanism.

Structure (following TESTING.md integration test patterns):
1. Import section: pytest, time, unittest.mock (Mock, patch), transcribe functions
2. Test class: class TestTimeoutIntegration with pytest.mark.integration decorator
3. Two test methods (requirements SIGNAL-07, SIGNAL-08):

test_retry_logic_with_timeout_on_first_attempt():
- Use @patch('transcribe.transcribe_image') to mock the function
- Set side_effect: [TimeoutError("Timeout after 60s"), ("Success text", 45.0, Mock())]
- First call times out, second succeeds
- Create mock VertexAIClient and call transcribe()
- Assert: result is "Success text"
- Assert: mock_transcribe_image.call_count == 2 (retry happened)
- Verifies SIGNAL-07 (timeout on first attempt triggers retry)

test_all_retries_exhausted_with_timeout():
- Create mock genai_client with models.generate_content that sleeps 10 seconds (simulating never-completing API)
- Use short timeout values for test speed (0.5s, 1s, 1.5s instead of 60/120/300)
- Mock timeout_seconds_list to use short values
- Call transcribe_image()
- Assert pytest.raises(TimeoutError, match="after 3 retries") or similar message
- Verifies SIGNAL-08 (all retries exhausted scenario)

Follow patterns from tests/integration/test_local_mode.py and test_ai_clients.py for mocking and assertions.

Use descriptive docstrings explaining integration scenario and expected behavior.

Add @pytest.mark.integration decorator to all tests per TESTING.md conventions.

IMPORTANT: Use short timeout values (< 2s) in tests to keep test suite fast. Real exponential backoff (60/120/300s) would make tests unbearably slow.
  </action>
  <verify>
1. Read tests/integration/test_timeout_integration.py and confirm 2 test methods exist
2. Run integration tests: pytest tests/integration/test_timeout_integration.py -v
3. Verify tests pass in reasonable time (< 30 seconds total)
4. Check test markers: pytest tests/integration/test_timeout_integration.py -v -m integration (should run both tests)
  </verify>
  <done>
tests/integration/test_timeout_integration.py exists with 2 passing integration tests verifying retry on first timeout and all retries exhausted. Tests complete in < 30 seconds using short timeout values. All tests marked with @pytest.mark.integration.
  </done>
</task>

</tasks>

<verification>
1. Install test dependencies: pip install -r requirements.txt
2. Run all new tests: pytest tests/unit/test_timeout_mechanism.py tests/integration/test_timeout_integration.py -v
3. Verify all 6 tests pass (4 unit + 2 integration)
4. Check test markers work: pytest -m unit -v (should include 4 timeout tests), pytest -m integration -v (should include 2 timeout tests)
5. Verify test speed: pytest tests/unit/test_timeout_mechanism.py tests/integration/test_timeout_integration.py --durations=0 (total < 30s)
6. Verify markers defined in .pytest.ini: grep -E "(unit|integration)" .pytest.ini
</verification>

<success_criteria>
1. requirements.txt contains pytest-mock, pytest-timeout, freezegun with version constraints
2. tests/unit/test_timeout_mechanism.py exists with 4 passing unit tests
3. tests/integration/test_timeout_integration.py exists with 2 passing integration tests
4. All tests use pytest.mark decorators (unit, integration) per TESTING.md conventions
5. Tests complete in < 30 seconds (no long delays)
6. Test coverage includes: timeout firing, cancellation, cleanup, exponential backoff, retry on timeout, retry exhaustion
7. SIGNAL-05, SIGNAL-06, SIGNAL-07, SIGNAL-08 requirements satisfied through test coverage
</success_criteria>

<output>
After completion, create `.planning/phases/01-cross-platform-timeout-fix/01-02-SUMMARY.md`
</output>
