---
phase: 01-cross-platform-timeout-fix
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - tests/platform/test_windows_compatibility.py
  - .github/workflows/test.yml
autonomous: true
requirements:
  - SIGNAL-09
  - SIGNAL-10

must_haves:
  truths:
    - "Platform-specific test verifies signal.SIGALRM not used on Windows"
    - "Platform-specific test verifies transcribe module imports successfully on Windows"
    - "GitHub Actions CI runs tests on windows-latest, ubuntu-latest, macos-latest"
    - "CI workflow executes all test markers (unit, integration, platform)"
    - "Test matrix shows Windows platform results"
  artifacts:
    - path: "tests/platform/test_windows_compatibility.py"
      provides: "Windows-specific platform tests"
      exports: ["test_no_signal_module_imports", "test_transcribe_runs_on_windows"]
      min_lines: 40
    - path: ".github/workflows/test.yml"
      provides: "GitHub Actions CI with cross-platform test matrix"
      contains: "windows-latest"
      min_lines: 30
  key_links:
    - from: ".github/workflows/test.yml"
      to: "tests/platform/test_windows_compatibility.py"
      via: "pytest execution on windows-latest runner"
      pattern: "pytest.*-m.*platform"
    - from: "tests/platform/test_windows_compatibility.py"
      to: "transcribe.py"
      via: "source code inspection for signal usage"
      pattern: "inspect\\.getsource"
---

<objective>
Add Windows-specific platform tests and GitHub Actions CI to verify cross-platform compatibility.

Purpose: Satisfy SIGNAL-09 (run tests on Windows platform) and SIGNAL-10 (verify no AttributeError on Windows). CI provides automated regression prevention and continuous validation across platforms.

Output: Platform test file verifying Windows compatibility through source code inspection and import tests. GitHub Actions workflow running full test suite on ubuntu-latest, macos-latest, and windows-latest.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cross-platform-timeout-fix/01-RESEARCH.md
@.planning/phases/01-cross-platform-timeout-fix/01-01-SUMMARY.md
@.planning/phases/01-cross-platform-timeout-fix/01-02-SUMMARY.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@transcribe.py
@.pytest.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Windows platform compatibility tests</name>
  <files>tests/platform/test_windows_compatibility.py</files>
  <action>
Create tests/platform/test_windows_compatibility.py with platform-specific tests verifying Windows compatibility.

Directory creation: If tests/platform/ directory doesn't exist, create it with __init__.py file.

Structure (following TESTING.md patterns):
1. Import section: sys, pytest, inspect, unittest.mock (Mock), transcribe module
2. Test class: class TestWindowsCompatibility
3. Two test methods:

test_no_signal_module_imports():
- Add marker: @pytest.mark.skipif(sys.platform != "win32", reason="Windows-specific test")
- Import transcribe module
- Use inspect.getsource(transcribe.transcribe_image) to get function source code
- Assert "signal.SIGALRM" not in source
- Assert "signal.alarm" not in source
- Assert "threading.Timer" in source (verify replacement used)
- Verifies SIGNAL-10 (signal module not referenced)
- Follow pattern from 01-RESEARCH.md lines 540-563

test_transcribe_runs_on_windows():
- Add marker: @pytest.mark.skipif(sys.platform != "win32", reason="Windows-specific test")
- Import VertexAIClient from transcribe (or similar client class)
- Create mock genai client: mock_client = Mock()
- Instantiate: client = VertexAIClient(mock_client, "gemini-3-flash-preview")
- Assert client is not None (should not raise AttributeError during import/instantiation)
- Verifies SIGNAL-10 (no AttributeError on Windows)
- Follow pattern from 01-RESEARCH.md lines 555-564

Add module docstring:
"""
Platform-specific tests for Windows compatibility.

These tests verify that the transcription tool can run on Windows without
AttributeError from Unix-only signal.SIGALRM. Tests are skipped on non-Windows
platforms using pytest.mark.skipif.
"""

Add custom pytest marker to .pytest.ini if not present:
```ini
markers =
    ...existing markers...
    platform: Platform-specific compatibility tests
```

Note: These tests will be SKIPPED on Unix/macOS platforms - that's expected. They only run on Windows (sys.platform == "win32").
  </action>
  <verify>
1. Check tests/platform/ directory exists with __init__.py
2. Read tests/platform/test_windows_compatibility.py and confirm 2 test methods
3. Verify skipif decorators present with sys.platform != "win32"
4. Check .pytest.ini has 'platform' marker defined
5. Run on non-Windows: pytest tests/platform/test_windows_compatibility.py -v (should show 2 SKIPPED tests if not on Windows)
  </verify>
  <done>
tests/platform/test_windows_compatibility.py exists with 2 platform-specific tests that verify signal.SIGALRM removal and Windows import success. Tests skip on non-Windows platforms. Platform marker added to .pytest.ini.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI workflow</name>
  <files>.github/workflows/test.yml</files>
  <action>
Create .github/workflows/test.yml with GitHub Actions workflow for cross-platform testing.

Directory creation: If .github/workflows/ directory doesn't exist, create it.

Workflow structure:
```yaml
name: Test Suite

on:
  push:
    branches: [ master, main, develop ]
  pull_request:
    branches: [ master, main, develop ]

jobs:
  test:
    name: Test on ${{ matrix.os }} with Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.12']

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run unit tests
      run: |
        pytest tests/unit/ -v -m unit

    - name: Run integration tests
      run: |
        pytest tests/integration/ -v -m integration

    - name: Run platform tests
      run: |
        pytest tests/platform/ -v -m platform

    - name: Run full test suite
      run: |
        pytest tests/ -v --tb=short
```

Key features:
- Matrix strategy with 3 platforms: ubuntu-latest, macos-latest, windows-latest
- Python 3.12 (matches project requirements)
- fail-fast: false (run all platforms even if one fails)
- Separate steps for unit, integration, platform tests (clear CI output)
- Final step runs full suite for comprehensive validation

Satisfies SIGNAL-09: Tests run on Windows platform (windows-latest runner).

IMPORTANT: This workflow runs on every push/PR to master/main/develop branches. Adjust branch names if project uses different convention.
  </action>
  <verify>
1. Check .github/workflows/ directory exists
2. Read .github/workflows/test.yml and confirm matrix includes windows-latest
3. Verify steps include pytest execution for unit, integration, platform tests
4. Validate YAML syntax: python3 -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml'))"
5. Check workflow triggers (push, pull_request) on correct branches
  </verify>
  <done>
.github/workflows/test.yml exists with GitHub Actions workflow running tests on ubuntu-latest, macos-latest, and windows-latest. Workflow includes separate steps for unit, integration, and platform tests. YAML syntax valid.
  </done>
</task>

</tasks>

<verification>
1. Check platform test structure: ls -la tests/platform/
2. Run platform tests locally (will skip on non-Windows): pytest tests/platform/test_windows_compatibility.py -v
3. Verify skipif logic: python3 -c "import sys; print(f'Platform: {sys.platform}')" (if not "win32", tests should skip)
4. Validate GitHub Actions workflow YAML: python3 -c "import yaml; yaml.safe_load(open('.github/workflows/test.yml'))"
5. Check workflow includes windows-latest: grep "windows-latest" .github/workflows/test.yml
6. Verify all test markers used in workflow: grep "pytest.*-m" .github/workflows/test.yml (should see unit, integration, platform)
</verification>

<success_criteria>
1. tests/platform/test_windows_compatibility.py exists with 2 platform-specific tests
2. Both tests use @pytest.mark.skipif(sys.platform != "win32") decorator
3. Tests verify signal.SIGALRM removal and Windows import success
4. .github/workflows/test.yml exists with matrix strategy
5. Workflow runs on ubuntu-latest, macos-latest, windows-latest
6. Workflow executes unit, integration, and platform test markers
7. SIGNAL-09 satisfied: GitHub Actions provides Windows testing capability
8. SIGNAL-10 satisfied: Platform tests verify no AttributeError on Windows
9. Platform marker added to .pytest.ini markers list
</success_criteria>

<output>
After completion, create `.planning/phases/01-cross-platform-timeout-fix/01-03-SUMMARY.md`
</output>
